name: Terraform Deploy to test

on:
  workflow_run:
    workflows: ["Terraform Deploy to dev"]
    types: [completed]
    branches: [main]

permissions:
  contents: read

jobs:
  deploy-test:
    name: Deploy to Test
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    environment: test
    concurrency: deploy-terraform-${{ github.ref }}-${{ github.workflow }}-test
    defaults:
      run:
        working-directory: iac/terraform
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ~1.0

      - name: Download Plan Artifact
        uses: actions/download-artifact@v4
        with:
          name: terraform-plan
          path: iac/terraform/
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id }}

      - name: Terraform Init
        env:
          TF_TOKEN_app_terraform_io: ${{ secrets.TFC_TOKEN }}
        run: terraform init

      - name: Terraform Apply
        env:
          TF_TOKEN_app_terraform_io: ${{ secrets.TFC_TOKEN }}
          TF_WORKSPACE: test
        run: |
          if [ -f "tfplan" ]; then
            terraform apply -input=false tfplan
          else
            echo "Plan artifact not found, running plan and apply"
            terraform plan -out=tfplan
            terraform apply -input=false tfplan
          fi

      - name: Terraform Output
        env:
          TF_TOKEN_app_terraform_io: ${{ secrets.TFC_TOKEN }}
        run: terraform output -json > terraform-outputs-test.json

      - name: Upload Outputs
        uses: actions/upload-artifact@v4
        with:
          name: terraform-outputs-test
          path: iac/terraform/terraform-outputs-test.json